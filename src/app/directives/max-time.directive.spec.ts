import {MinTimeValidator} from './min.directive';

describe('MinDirective', () => {
  it('should create an instance', () => {
    const directive = new MinTimeValidator();
    expect(directive).toBeTruthy();
  });
});
